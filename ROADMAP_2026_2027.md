# Unified Project Roadmap: April 2026 - March 2027

**Historical planning record. Superseded 2026-06-15.**

This document is preserved for lineage across BuildLang, Aurora, and APPS, but
it is no longer the active execution plan for BuildLang. Current BuildLang
posture: C remains the product anchor; Rust remains an experimental validation
lane; x86/x64 remains preserved backend research; full-scale compiler/language
expansion is paused. See
`docs/COMPILER_WIND_DOWN_ASSESSMENT_2026-06-15.md` and `STATUS.md` for current
compiler evidence.

Covers: BuildLang (BUILD-UNIVERSE), Aurora Framework (WOW), and APPS (maintenance).

---

## What's Done

### APPS - Complete (maintenance mode)
- 5 production Python apps: calibrate-pro, build-color, build-finance, build-oracle, build-engine
- 277 Python files, all with PyQt6 GUIs, CLIs, PyInstaller executables, test suites
- These are reference implementations. No new features needed.

### BuildLang Compiler - Current Verified Posture
- 88,946 Rust source lines in `compiler/src/`; 100,171 tracked Rust lines under `compiler/`
- 868-test CI-shaped compiler baseline as of 2026-06-15
- C backend is the production anchor; HLSL/GLSL are useful public adjuncts;
  Rust, SPIR-V, LLVM, WASM, x86-64, and ARM64 are experimental or validation lanes
- HLSL/GLSL shader-output fixtures remain useful for public examples; SPIR-V
  graphics artifacts are preserved as experimental backend history
- `tex2d()` texture sampling, `#[fragment]` auto-wiring, ReShade `.fx`
  generation, color-space type experiments, and the RTX 4090 Vulkan demo are
  historical graphics-sprint artifacts, not current release gates
- Python app ports and larger `.bld` programs are preserved as mixed
  fixture/design corpus material, not current adoption proof

### Aurora Framework - 85-90% Complete
- 1,018 Lua files, 143K+ lines, 67-module SDK
- All 13 WoW classes / 47 specs covered with 57 production rotations
- DSL compiler (4-stage pipeline), TypeScript integration, 10 documentation guides
- Core systems complete: spell/unit caching, tick engine, DR tracking, interrupts, content adapters

---

## Q2 2026 (April - June): Ship Aurora + BuildLang Output Quality

**Theme: Get one product out the door. Make BuildLang output professional.**

### Aurora: Ship It (Weeks 1-8)

| Week | Deliverable | Detail |
|------|-------------|--------|
| 1-2 | **In-game validation** | Install Aurora in WoW 11.1. Load 5 rotations (1 tank, 1 healer, 1 melee DPS, 1 ranged DPS, 1 PvP). Fix API call failures, timing issues, nil errors. |
| 3-4 | **Fix and polish** | Fix all issues found in week 1-2. Validate the remaining 52 rotations load without error. Performance profile the tick engine (target: <1ms per tick). |
| 5-6 | **Packaging** | Build the addon distribution: `.toc` file, `libs/` embedded dependencies, `Aurora-Install.bat`. Package for CurseForge/WoWInterface submission format. |
| 7-8 | **Distribution** | Upload to WoWInterface or private GitHub release. Write a 1-page landing README with install instructions + quickstart. Create 2-minute demo video showing a rotation in action. |

**Exit criteria:** Aurora addon installable and functional in WoW 11.1 for at least 10 specs.

### BuildLang: Output Optimization (Weeks 1-6, parallel with Aurora)

| Deliverable | Detail |
|-------------|--------|
| **Deep copy propagation** | Extend peephole to handle Call terminator → statement chains. Eliminate 80%+ of `_temp` variables from HLSL/GLSL output. |
| **Dead declaration elimination** | Track which locals are actually referenced in the final output. Only declare used variables. |
| **Expression inlining** | For single-use temps that are simple expressions (field access, constants), inline them directly: `float _8 = color.x;` `float _9 = (_8 * vig);` → `float _9 = (color.x * vig);` |
| **Parenthesis cleanup** | Remove redundant parens: `(_8 * vig)` → `_8 * vig` where precedence is unambiguous. |

**Target output for vignette_shader.bld:**
```hlsl
float4 PS_Vignette(float4 pos : SV_Position, float2 uv : TEXCOORD) : SV_Target0 {
    float4 color = tex2D(ReShade::BackBuffer, uv);
    float vig = vignette(color.x, color.y, 0.5, 0.6);
    return float4(color.x * vig, color.y * vig, color.z * vig, 1.0);
}
```

**Exit criteria:** Generated HLSL for the vignette shader is <20 lines with no unused variables.

### BuildLang: Uniform Support (Weeks 7-10)

| Deliverable | Detail |
|-------------|--------|
| **`#[uniform]` attribute** | Parse `#[uniform] let exposure: f64 = 1.0;` at module level. |
| **HLSL uniform generation** | Emit `uniform float Exposure < ui_type = "slider"; ... > = 1.0;` for ReShade. |
| **GLSL uniform generation** | Emit `uniform float Exposure = 1.0;` |
| **Multiple textures** | `tex2d_named("depth", uv)` → `tex2D(DepthBuffer, uv)` for sampling non-backbuffer textures. |

**Exit criteria:** Full cinematic shader with user-adjustable uniforms compiles from BuildLang source.

---

## Q3 2026 (July - September): Killer Demo + Developer Experience

**Theme: Write the shader that makes graphics programmers stop scrolling. Make BuildLang usable by someone who isn't you.**

### BuildLang: The Killer Shader (Weeks 1-4)

Write one of these ENTIRELY in BuildLang, compile to `.fx`, verify it works in ReShade:

| Option | Difficulty | Visual Impact |
|--------|-----------|---------------|
| **Screen-Space Ambient Occlusion (SSAO)** | Medium | High - darkens crevices, adds depth |
| **Gaussian Blur (multi-pass)** | Easy | Medium - foundation for DoF, bloom |
| **Depth of Field** | Medium | Very high - cinematic bokeh |
| **Volumetric Light Shafts** | Hard | Very high - god rays |

**Pick one.** SSAO is the best balance of difficulty and impressiveness.

Requirements for the SSAO shader:
- Sample depth buffer via `tex2d_named("depth", uv)`
- Sample normal buffer (reconstruct from depth if needed)
- Random kernel sampling (hemisphere)
- Bilateral blur pass for denoising
- Blend with scene color

**Exit criteria:** `buildc ssao.bld --target hlsl -o ssao.fx` produces a working ReShade SSAO effect.

### BuildLang: VS Code Extension (Weeks 3-6, historical)

| Deliverable | Detail |
|-------------|--------|
| **Syntax highlighting** | TextMate grammar for `.bld` files: keywords, types, attributes, comments, strings. |
| **Snippets** | `fn`, `struct`, `impl`, `#[fragment]`, `#[vertex]`, `effect`, `with`. |
| **Wire existing LSP** | Historical item. `buildc lsp` now exists, but the server loop still dispatches only lifecycle messages; real completion/hover/diagnostic requests are not yet connected. |
| **Basic diagnostics** | Historical target. Current reliable editor features are syntax/snippets/brackets; LSP diagnostics are not yet connected to real client requests. |
| **Publish to VS Code Marketplace** | Historical target. Extension sources are retained in `editors/vscode`; marketplace availability is not a current claim. |

**Historical exit criteria:** A developer could install the extension, open a
`.bld` file, see syntax highlighting, and get error diagnostics. Current
evidence only supports syntax/snippet/editor chrome plus partial `buildc lsp`
process launch.

### BuildLang: Getting Started Guide (Weeks 5-8)

| Deliverable | Detail |
|-------------|--------|
| **Install guide** | Download compiler binary (Windows/Linux/macOS), add to PATH. |
| **Hello Shader tutorial** | 5-minute guide: write a tone mapper, compile to `.fx`, drop in ReShade. |
| **Language reference** | Types, functions, structs, enums, generics, effects, `with` annotations, `#[fragment]`/`#[vertex]`. |
| **Shader cookbook** | 10 common shader patterns in BuildLang: vignette, blur, color grading, noise, edge detection. |
| **Host on GitHub Pages** | `buildlang.dev` or `github.io/buildlang` - landing page + docs. |

**Exit criteria:** A shader programmer can go from zero to a working ReShade effect in under 10 minutes using the docs.

### BuildLang: HLSL While Loops (Week 9-10)

| Deliverable | Detail |
|-------------|--------|
| **While loop emission** | MIR loop blocks → `while (cond) { body }` in HLSL/GLSL. |
| **For loop emission** | MIR counted loops → `for (int i = 0; i < N; i++)` in HLSL/GLSL. |

**Exit criteria:** Kernel sampling loops in the SSAO shader compile to clean HLSL while/for loops.

---

## Q4 2026 (October - December): GPU Pipeline + Vulkan

**Theme: BuildLang programs render pixels without a C host.**

### BuildLang: MIR → SPIR-V Coverage (Weeks 1-5)

| Deliverable | Detail |
|-------------|--------|
| **Fix remaining MIR operations** | FieldAccess, pointer stores, globals - the ~50% of ops that currently return zero stubs. |
| **Source-compiled fragment shaders** | `buildc frag.bld --target spirv` produces valid SPIR-V (spirv-val passes) from BuildLang source through AST→MIR→SPIR-V. |
| **PBR BRDF in SPIR-V** | Compile the test 75 PBR material function to SPIR-V. Validate with spirv-val. |

**Exit criteria:** `buildc pbr_shade.bld --target spirv -o pbr.spv` validates clean.

### BuildLang: Minimal Vulkan Host (Weeks 6-9)

| Deliverable | Detail |
|-------------|--------|
| **vulkan_host.c expansion** | Extend the existing ~300-line Vulkan host to support fragment shaders, push constants, descriptor sets for textures. |
| **C FFI from BuildLang** | `vk_init()`, `vk_create_pipeline(vert_spv, frag_spv)`, `vk_draw()`, `vk_present()` callable from BuildLang. |
| **Window + render loop** | Win32 window + Vulkan swapchain + render loop, driven from BuildLang `main()`. |

**Exit criteria:** A BuildLang program opens a window and renders a textured quad using source-compiled SPIR-V shaders.

### BuildLang: Cross-Target Verification (Weeks 10-12)

| Deliverable | Detail |
|-------------|--------|
| **Bit-identical math** | Compile `srgb_to_linear()` to C (CPU) and SPIR-V (GPU). Run both with identical inputs. Compare outputs. |
| **Automated cross-target test** | Test harness that compiles a function to C + HLSL + GLSL + SPIR-V, runs the C version, and verifies the shader outputs match. |
| **Test suite expansion** | 120+ end-to-end tests (up from 117). Add shader-specific tests. |

**Exit criteria:** 5 color math functions produce verified-identical results across C, HLSL, and GLSL backends.

---

## Q1 2027 (January - March): Type System Power + Ecosystem

**Theme: The compiler catches bugs no other tool can find.**

### BuildLang: Color Space Enforcement in Shaders (Weeks 1-4)

| Deliverable | Detail |
|-------------|--------|
| **Annotations flow through HLSL/GLSL** | `fn tonemap(c: vec3 with ColorSpace<Linear>) -> vec3 with ColorSpace<sRGB>` - the annotation is preserved in the generated shader comments. |
| **Compile error on mixing** | `let wrong = pbr_shade(my_srgb_color)` where `pbr_shade` expects Linear → compiler error with clear message. |
| **Built-in color space types** | `type LinearRGB = vec3 with ColorSpace<Linear>`, `type sRGBColor = vec3 with ColorSpace<sRGB>` in the stdlib. |
| **Conversion functions** | `srgb_to_linear()` changes the annotation from `sRGB` to `Linear`. Verified at compile time. |

**Exit criteria:** Passing sRGB color to a function expecting Linear produces a clear compiler error.

### BuildLang: Precision Annotations (Weeks 5-8)

| Deliverable | Detail |
|-------------|--------|
| **`with Precision<Full/Half>`** | Annotation on float types tracking precision level. |
| **Precision warnings** | Compiler warns when precision drops below a threshold in arithmetic chains. |
| **HLSL `half` emission** | `vec3 with Precision<Half>` emits `half3` in HLSL, `mediump vec3` in GLSL. |

**Exit criteria:** A 10-operation chain with `Half` precision produces a compiler warning about potential precision loss.

### BuildLang: Package Manager + Registry (Weeks 9-12)

| Deliverable | Detail |
|-------------|--------|
| **Wire `buildc pkg` CLI** | Historical item. `buildc pkg` now exists, but there is no live package registry. |
| **`build.toml` manifest** | Project metadata, dependencies, build targets. |
| **Local dependencies** | `buildc pkg add ../spectrum` resolves local path dependencies. |
| **Shader library** | Publish `build-shaders` package with vignette, tonemap, blur, noise, SSAO. |

**Exit criteria:** `buildc pkg init && buildc pkg add build-shaders && buildc build` works for a new project.

---

## Aurora Maintenance (Q3-Q1, as needed)

After the Q2 ship:
- **Bug fixes** from user reports (ongoing)
- **Patch compatibility** - update spell IDs when WoW patches (quarterly)
- **Premium features** - routine builder, arena coaching (if demand warrants)
- **Community rotations** - review and merge community-contributed rotations

Aurora is a **ship and maintain** product. The heavy development is done.

---

## APPS Maintenance (entire year)

- No new features.
- Update dependencies if security vulnerabilities are found.
- Keep as reference for validating BuildLang ports.

---

## Success Metrics

| Quarter | Metric | Target |
|---------|--------|--------|
| Q2 | Aurora shipped | Installable addon, 10+ working specs |
| Q2 | HLSL output quality | Vignette shader < 20 lines, no unused vars |
| Q3 | Killer demo | SSAO shader working in ReShade |
| Q3 | VS Code extension | Published on marketplace with LSP |
| Q3 | Getting started guide | Zero-to-shader in 10 minutes |
| Q4 | SPIR-V from source | PBR function validates with spirv-val |
| Q4 | Vulkan rendering | BuildLang program renders textured quad |
| Q4 | Cross-target verification | 5 functions bit-identical across backends |
| Q1 | Color space errors | sRGB→Linear mixing caught at compile time |
| Q1 | Package manager | `buildc pkg` works end-to-end |
| Full year | Compiler size | Superseded: 88,946 source lines in `compiler/src/` as of 2026-06-15 |
| Full year | Test count | Superseded: 868-test CI-shaped baseline; legacy `buildc test` is not the release gate |
| Full year | Backends | Superseded: C production anchor; other backends carry explicit experimental maturity |

---

## Files Modified by This Roadmap

**BuildLang Compiler:**
- `compiler/src/codegen/backend/hlsl.rs` - output optimization, uniform emission, while loops
- `compiler/src/codegen/backend/glsl.rs` - same
- `compiler/src/codegen/backend/spirv.rs` - MIR operation coverage
- `compiler/src/codegen/lower.rs` - uniform lowering, loop MIR
- `compiler/src/codegen/runtime.rs` - Vulkan FFI
- `compiler/src/types/infer.rs` - precision annotations, uniform parsing
- `compiler/src/types/unify.rs` - color space enforcement
- `compiler/src/parser/ty.rs` - precision type syntax
- `compiler/src/main.rs` - CLI subcommands (lsp, pkg, fmt)

**New Files:**
- `editors/vscode/` - VS Code extension (TextMate grammar, LSP client)
- `docs/getting-started.md` - tutorial
- `docs/language-reference.md` - reference
- `demos/ssao.bld` - killer demo
- `stdlib/shaders/` - shader utility library

**Aurora:**
- `aurora/dist/` - packaged addon
- `aurora/*.toc` - WoW addon manifest

---

## Priority Stack (if time is limited)

If you can only do 5 things this year:

1. **Ship Aurora** - nearest to revenue, 85% done, just needs testing + packaging
2. **Output optimization** - makes BuildLang credible to graphics programmers
3. **VS Code extension** - nobody tries a language without editor support
4. **SSAO killer demo** - the proof that BuildLang writes real shaders
5. **Getting started guide** - the bridge from "interesting" to "I'll try it"

Everything else is leverage on these five.
