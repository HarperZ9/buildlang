struct Point {
    x: i32,
    y: i32,
}

struct Holder {
    item: Point,
}

fn main() {
    let p = Point { x: 3, y: 4 };
    let mut holder = Holder { item: p };
    holder.item = p;
    let again = p;
    println("{}", holder.item.x + again.y);
}
