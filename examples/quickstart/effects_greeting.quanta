effect Greeting {
    fn greet(name: str) -> (),
}

fn welcome() ~ Greeting {
    perform Greeting.greet("teammate");
}

fn main() ~ Console {
    handle {
        welcome()
    } with {
        Greeting.greet(name) => {
            println!("Hello, {}!", name)
        },
    }
}
